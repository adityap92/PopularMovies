<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="android:Theme.Light">
        <!-- Customize your theme here. -->
    </style>
    <color name="custom_theme_color">#80CBC4</color>
    <style name="MyTheme" parent="android:Theme.Material">
        <!-- Main theme colors -->
        <!--   your app branding color for the app bar -->
        <item name="android:colorPrimary" type="color">#009688</item>

        <!--   darker variant for the status bar and contextual app bars -->
        <item name="android:colorPrimaryDark" type="color">#00796B</item>
        <!--   theme UI controls like checkboxes and text fields -->
        <item name="android:colorAccent" type="color">#1DE9B6</item>
        <item name="android:colorBackground" type="color">#80CBC4</item>

        <item name="android:windowBackground">@color/custom_theme_color</item>

    </style>

</resources>
